$route
2s+1=1,maxsiz=900000
print=(gradient=(total))
hf scf=(pulay), guess=chk, geom=rdchk
frequencies
force-constants=(numerical=(cartesian,two-point))
basis=dz,sym=off
$title
 h2o test case: closed shell finite-difference force constants
                / cartesian two-point
$end
